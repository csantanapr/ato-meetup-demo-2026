apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: argocd-agent
  namespace: kagent
spec:
  declarative:
    modelConfig: bedrock-anthropic-claude-4-5-sonnet
    stream: false
    systemMessage: |-
      You are a GitOps expert with ArgoCD tools availabe
    tools:
      - type: McpServer
        mcpServer:
          apiGroup: kagent.dev
          kind: MCPServer
          name: argocd-mcp-server
          toolNames: []
  description: "A smart assistant for gitops"
  type: Declarative
